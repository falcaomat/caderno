# Linguagens e Extensões

Assembly `.asm` 
Bash | `.sh` 
Batch | `.bat`, `.cmd` 
C | `.c` 
C# | `.cs` 
C++ | `.cpp`, `.cc`, `.cxx` 
Clojure | `.clj` 
COBOL | `.cob`, `.cbl` 
Dart | `.dart` 
Elixir | `.ex`, `.exs` 
Erlang | `.erl`, `.hrl` 
Fortran | `.f`, `.f90`, `.f95`, `.f03` 
Go | `.go` 
Groovy | `.groovy` 
Haskell | `.hs`, `.lhs` 
Java | `.java` 
JavaScript | `.js`, `.mjs`, `.cjs` 
Julia | `.jl` 
Kotlin | `.kt`, `.kts` 
Lua | `.lua` 
MATLAB | `.m` 
Objective-C | `.m`, `.h` 
Perl | `.pl`, `.pm` 
PHP | `.php` 
PowerShell | `.ps1` 
Python | `.py` 
R | `.r`, `.R` 
Ruby | `.rb` 
Rust | `.rs` 
Scala | `.scala` 
Shell | `.sh` 
Swift | `.swift` 
TypeScript | `.ts`, `.tsx` 
Visual Basic | `.vb` 
Zig | `.zig` 

# Configuração, Marcação e Infraestrutura

HTML | `.html`, `.htm` 
CSS | `.css` 
SCSS | `.scss` 
Sass | `.sass` 
XML | `.xml` 
JSON | `.json` 
YAML | `.yaml`, `.yml` 
TOML | `.toml` 
INI | `.ini` 
Markdown | `.md`, `.markdown` 
Dockerfile | `Dockerfile` 
Docker Compose | `.yaml`, `.yml` 
Terraform | `.tf`, `.tfvars` 
Ansible | `.yaml`, `.yml` 
Kubernetes | `.yaml`, `.yml` 
Nginx | `.conf` 
Apache | `.conf` 
Makefile | `Makefile` 
Gitignore | `.gitignore` 
EditorConfig | `.editorconfig` 
Env | `.env` 
SQL | `.sql` 
GraphQL | `.graphql`, `.gql` 